@{
    ViewBag.Title = "Confirm Email";
}

<h2>@ViewBag.Title.</h2>
<div>
    @if (ViewData.ModelState[""] == null)
    {
    <p>
        验证成功 请登陆 @Html.ActionLink("click here to log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })
    </p>
    }
    else
    {
        <ul class="text-danger">
            @foreach(var error in ViewData.ModelState[""].Errors)
            {
                <li>@error</li>
            }
        </ul>
    }
</div>
